[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "benchflow"
version = "0.1.0"
requires-python = ">=3.12"

[project.optional-dependencies]
dev = [
    "mypy",
    "pytest",
    "ruff",
]

[tool.mypy]
python_version = "3.12"
